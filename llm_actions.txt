idea --> use an LLM to efficiently and naturally search action space
actions:
    - UPDATE/READ PREFERNCE DATA ACTIONS:
        - add_pairwise(a1 > a2): adds a pariwse pref for two actions
        - add_coactive(abar > a2): adds coactive push
        - add_ordinal(a): assigns ordinal label to action 
        - reset(d): resets the current dataset of preferences
        - conflicts(a): get conflicting pairs 

    - SAMPLING ACTIONS
        - thompson(): get action pair from thompson sampling
        - landscape(): get action pair from active learning
        - information(): get action pair to max out info gain
        - random(): get random action pair

    - NLP ACTIONS:
        - query(a1, a2): query actions to user
        - more(): ask for more NL feedback
        - clinician(): find semantically similar clinican data to feedback
        - skip(): skip iteration due to uninformative/useless feedback 